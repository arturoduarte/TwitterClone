<%= form_with(model: tweet, local: true) do |form| %>
  <% if tweet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tweet.errors.count, "error") %> prohibited this tweet from being saved:</h2>

      <ul>
      <% tweet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre, id: :tweet_nombre, autofocus: true %>
  </div>

  <div class="field">
      <%= form.label :titulo %>
      <%= form.text_field :titulo, id: :tweet_titulo %>
    </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
